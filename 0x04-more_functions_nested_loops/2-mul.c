#include "main.h"
/**
 * mul - multiplies 2 integers
 * @a: first integer
 * @b: second integer
 * Return: results of the multplication of the two integers.
 */
int mul(int a, int b)
{
	return (a * b);
}
